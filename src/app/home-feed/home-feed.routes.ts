import { Route } from '@angular/router';
import { HomeFeedComponent } from './home-feed.component';

export const homeFeedRoutes: Route[] = [
    {
        path: '',
        component: HomeFeedComponent,
    },
];
