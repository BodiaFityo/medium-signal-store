//Feature-based structure
/app

    /article
        /components
        /services
        /models
        /constants
        /store
    article.routes

    /global-feed
        /components
        /services
        /models
        /constants
        /store
    global-feed.routes

    /shared
        /components
        /services
        /models

    app.routes

main.ts
